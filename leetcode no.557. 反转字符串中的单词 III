class Solution(object):
    def reverseWords(self, s):
        """
        :type s: str
        :rtype: str
        """
        ls = s.split()
        for i, s in enumerate(ls):
            ls[i] = s[::-1]
        return ' '.join(ls)
